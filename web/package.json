{
  "name": "alethia-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Match and deduplicate messy entity names against a reference list, in the browser, on your own machine.",
  "scripts": {
    "test": "node --test 'test/**/*.test.mjs'",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "node --test 'test/**/*.test.mjs' && node render_check.mjs"
  },
  "dependencies": {
    "@huggingface/transformers": "^4.2.0",
    "@radix-ui/react-icons": "^1.3.2",
    "@radix-ui/themes": "^3.3.0",
    "react": "^19.2.8",
    "react-dom": "^19.2.8"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^6.0.5",
    "global-jsdom": "^29.0.0",
    "jsdom": "^29.0.0",
    "vite": "^8.2.1"
  },
  "overrides": {
    "sharp": "^0.35.4",
    "adm-zip": "^0.6.0"
  }
}
