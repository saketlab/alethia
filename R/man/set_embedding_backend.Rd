% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/embedder.R
\name{set_embedding_backend}
\alias{set_embedding_backend}
\alias{get_embedding_backend}
\title{Register a backend that turns model names into embedding functions}
\usage{
set_embedding_backend(backend)

get_embedding_backend()
}
\arguments{
\item{backend}{A function taking a model name and returning a function
`f(texts) -> matrix`. `NULL` clears the registration.}
}
\value{
The previous backend, invisibly.
}
\description{
The package ships no model runtime of its own. Point this at whatever you use to
produce embeddings, e.g. a local ONNX session, an API client, or a cached matrix.
}
\examples{
bag <- function(x) {
  t(sapply(
    strsplit(tolower(x), ""),
    function(ch) tabulate(match(ch, letters), 26)
  ))
}
old <- set_embedding_backend(function(model_name) bag)
as_embedder("any-model-name")
set_embedding_backend(old)
}
