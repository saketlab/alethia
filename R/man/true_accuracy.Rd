% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate.R
\name{true_accuracy}
\alias{true_accuracy}
\title{Top-1 accuracy and MRR of an embedder against ground truth}
\usage{
true_accuracy(queries, references, truth, model, nil_threshold = NULL)
}
\arguments{
\item{queries}{Character vector of query strings.}

\item{references}{Character vector of candidate strings.}

\item{truth}{Correct reference per query, or `NA` for an open-world NIL query with no
correct answer. Real matching input is full of these: a cause of death that is not
a diagnosis, a drug outside the formulary.}

\item{model}{Anything [as_embedder()] accepts.}

\item{nil_threshold}{Cosine below which a top-1 match counts as an abstention.
Required when `truth` contains `NA`; ignored otherwise.}
}
\value{
A named list with `top1` and `mrr`, plus `nil_recall` and `answerable_top1`
  when the dataset contains NIL queries. Those two are separate because one accuracy
  number hides the trade-off: a model can look good by always abstaining, or never.
}
\description{
The labelled counterpart to the label-free metrics: given the correct reference for
each query, how often does the model retrieve it? [assess_models()] predicts this
without labels.
}
\examples{
bag <- function(x) {
  t(sapply(
    strsplit(tolower(x), ""),
    function(ch) tabulate(match(ch, letters), 26)
  ))
}
true_accuracy(
  c("colour", "flavour"), c("color", "flavor"),
  c("color", "flavor"), bag
)
}
