% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/embedder.R
\name{match_by_embeddings}
\alias{match_by_embeddings}
\title{Match queries to references by embedding cosine similarity}
\usage{
match_by_embeddings(queries, references, embedder, threshold = NULL)
}
\arguments{
\item{queries}{Character vector of strings to match.}

\item{references}{Character vector of candidate strings.}

\item{embedder}{Anything [as_embedder()] accepts.}

\item{threshold}{Optional minimum cosine. Matches below it become `NA`.}
}
\value{
A data frame with `given_entity`, `alethia_prediction`, `alethia_score`.
}
\description{
The single embedding-based matching primitive, mirroring
`alethia.embedder.match_by_embeddings`. Every embedding backend routes through it.
}
\examples{
bag <- function(x) {
  t(sapply(
    strsplit(tolower(x), ""),
    function(ch) tabulate(match(ch, letters), 26)
  ))
}
match_by_embeddings(c("colour"), c("color", "zebra"), bag)
}
