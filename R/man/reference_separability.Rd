% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assess.R
\name{reference_separability}
\alias{reference_separability}
\title{Reference-set separability}
\usage{
reference_separability(ref_emb, confusion_threshold = 0.9)
}
\arguments{
\item{ref_emb}{Numeric matrix of reference embeddings.}

\item{confusion_threshold}{Cosine above which two references count as confusable.}
}
\value{
Named list of `mean_nn_similarity`, `median_nn_similarity`,
  `confusability_rate`.
}
\description{
How well distinct references are kept apart. Returns the mean and median cosine to
the nearest *other* reference (lower is better), and the fraction whose nearest
other reference exceeds `confusion_threshold`.
}
\details{
These are raw cosines, so they partly reflect anisotropy: transformer embeddings
occupy a narrow cone, pushing every cosine high. Retrieval depends on the ordering
of similarities, not their level, so [centered_separability()] gives the
anisotropy-corrected view.
}
\examples{
bag <- function(x) {
  t(sapply(
    strsplit(tolower(x), ""),
    function(ch) tabulate(match(ch, letters), 26)
  ))
}
refs <- bag(c("aspirin", "ibuprofen", "paracetamol", "metformin"))
queries <- bag(c("asprin", "ibuprofn", "paracetmol", "metfornin"))
reference_separability(refs)
}
\seealso{
Other assess metrics:
\code{\link[=alignment_loss]{alignment_loss()}},
\code{\link[=centered_separability]{centered_separability()}},
\code{\link[=hubness]{hubness()}},
\code{\link[=intrinsic_dimensionality]{intrinsic_dimensionality()}},
\code{\link[=mutual_nn_rate]{mutual_nn_rate()}},
\code{\link[=positive_pair_rank]{positive_pair_rank()}},
\code{\link[=retrieval_margin]{retrieval_margin()}},
\code{\link[=uniformity_loss]{uniformity_loss()}}
}
\concept{assess metrics}
