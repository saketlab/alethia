% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/embedder.R
\name{encode}
\alias{encode}
\title{Encode text with an embedder}
\usage{
encode(embedder, texts)
}
\arguments{
\item{embedder}{An `alethia_embedder`, from [as_embedder()].}

\item{texts}{Character vector.}
}
\value{
Numeric matrix with one row per input.
}
\description{
Encode text with an embedder
}
\examples{
bag <- function(x) {
  t(sapply(
    strsplit(tolower(x), ""),
    function(ch) tabulate(match(ch, letters), 26)
  ))
}
dim(encode(as_embedder(bag), c("colour", "color")))
}
