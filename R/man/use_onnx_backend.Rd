% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/onnx.R
\name{use_onnx_backend}
\alias{use_onnx_backend}
\title{Resolve bare model names through the local ONNX runtime}
\usage{
use_onnx_backend(...)
}
\arguments{
\item{...}{Passed to [onnx_embedder()], e.g. `threads` or `batch_size`.}
}
\value{
The previously registered backend, invisibly.
}
\description{
After calling this, a model name passed to [alethia()], [cluster_entities()] or
[as_embedder()] is loaded and run locally on the CPU.
}
\examples{
\donttest{
if (onnx_available()) {
  use_onnx_backend(threads = 4)
  alethia(c("Bombay"), c("Mumbai", "Delhi"), model = "sentence-transformers/all-MiniLM-L6-v2")
}
}
}
