% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cluster.R
\name{cluster_entities}
\alias{cluster_entities}
\title{Group entity strings into clusters of duplicates}
\usage{
cluster_entities(
  entities,
  model,
  floor = 0.8,
  k = 5,
  require_mutual = TRUE,
  min_confidence = NULL,
  canonical = c("shortest", "first")
)
}
\arguments{
\item{entities}{Character vector to group. De-duplicated on exact text first.}

\item{model}{Anything [as_embedder()] accepts.}

\item{floor}{Minimum cosine for an edge.}

\item{k}{Neighbourhood size for the mutual-NN test. Clamped to `[1, n - 1]`.}

\item{require_mutual}{Require each entity to be in the other's top-k.}

\item{min_confidence}{Optional cutoff applied to edge confidence before clustering.}

\item{canonical}{How to name each cluster: `"shortest"` member or `"first"` seen.}
}
\value{
An object of class `alethia_clusters`.
}
\description{
Mirrors `alethia.cluster.cluster_entities`. Edges require *mutual*
nearest-neighbourhood above a cosine floor and each carries a confidence, so no hub
chains the whole set into one cluster the way a plain threshold does.
}
\examples{
bag <- function(x) {
  t(sapply(
    strsplit(tolower(x), ""),
    function(ch) tabulate(match(ch, letters), 26)
  ))
}
res <- cluster_entities(c("color", "colour", "zebra"), bag, floor = 0.8)
res
cluster_records(res)
}
