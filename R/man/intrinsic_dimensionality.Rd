% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assess.R
\name{intrinsic_dimensionality}
\alias{intrinsic_dimensionality}
\title{Intrinsic dimensionality}
\usage{
intrinsic_dimensionality(emb)
}
\arguments{
\item{emb}{Numeric matrix of embeddings.}
}
\value{
Named list of `participation_ratio` and `normalized_pr`.
}
\description{
Participation ratio `(sum(lambda))^2 / sum(lambda^2)` over the PCA eigenvalues, and
the same divided by the ambient dimension. Higher means the model spreads the data
over more directions.
}
\examples{
bag <- function(x) {
  t(sapply(
    strsplit(tolower(x), ""),
    function(ch) tabulate(match(ch, letters), 26)
  ))
}
refs <- bag(c("aspirin", "ibuprofen", "paracetamol", "metformin"))
queries <- bag(c("asprin", "ibuprofn", "paracetmol", "metfornin"))
intrinsic_dimensionality(refs)
}
\seealso{
Other assess metrics:
\code{\link[=alignment_loss]{alignment_loss()}},
\code{\link[=centered_separability]{centered_separability()}},
\code{\link[=hubness]{hubness()}},
\code{\link[=mutual_nn_rate]{mutual_nn_rate()}},
\code{\link[=positive_pair_rank]{positive_pair_rank()}},
\code{\link[=reference_separability]{reference_separability()}},
\code{\link[=retrieval_margin]{retrieval_margin()}},
\code{\link[=uniformity_loss]{uniformity_loss()}}
}
\concept{assess metrics}
