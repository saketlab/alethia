% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/onnx.R
\name{onnx_embedder}
\alias{onnx_embedder}
\title{Build an embedder backed by a local ONNX model}
\usage{
onnx_embedder(
  model_id,
  backend = c("cpu", "coreml", "cuda", "xnnpack", "openvino"),
  threads = NULL,
  max_length = 256L,
  batch_size = 32L,
  onnx_file = "onnx/model.onnx",
  tokenizer_file = "tokenizer.json"
)
}
\arguments{
\item{model_id}{Hugging Face model id, e.g. `"sentence-transformers/all-MiniLM-L6-v2"`.}

\item{backend}{Execution provider. `"cpu"` by default; the others are only useful
if you have the matching hardware and an ONNX Runtime built for it.}

\item{threads}{Inference threads. `NULL` leaves it to ONNX Runtime.}

\item{max_length}{Sequence length above which inputs are truncated.}

\item{batch_size}{Texts per forward pass. Lower this if memory is tight; it changes
speed and memory, not results.}

\item{onnx_file}{Path within the repository to the ONNX weights.}

\item{tokenizer_file}{Path within the repository to the tokeniser.}
}
\value{
An `alethia_embedder`, usable anywhere a model is accepted.
}
\description{
Runs a sentence-embedding model on the CPU with no Python installation, using
'onnxr' for inference, 'tok' for tokenisation, and 'hfhub' to fetch the weights.
}
\details{
Pooling reproduces sentence-transformers (an attention-masked mean over token
vectors, then L2 normalisation), so embeddings agree with the Python package to
floating-point tolerance for the same model.

The model must publish ONNX weights. Most sentence-transformers models do, under
`onnx/model.onnx`; the `onnx-community` and `Xenova` organisations on Hugging Face
publish conversions of many others.
}
\examples{
\donttest{
# Downloads ~90 MB on first use, then caches.
if (onnx_available()) {
  embedder <- onnx_embedder("sentence-transformers/all-MiniLM-L6-v2")
  alethia(c("Bombay", "Calcutta"), c("Mumbai", "Kolkata"), model = embedder)
}
}
}
\seealso{
[use_onnx_backend()] to resolve bare model names through this path.
}
