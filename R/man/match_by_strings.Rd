% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stringmatch.R
\name{match_by_strings}
\alias{match_by_strings}
\title{Match strings against a reference list by token-sort similarity}
\usage{
match_by_strings(queries, references, threshold = NULL)
}
\arguments{
\item{queries}{Character vector of strings to match.}

\item{references}{Character vector of candidate strings.}

\item{threshold}{Minimum score in `[0, 1]`; below it an entry is `NA`. `NULL`
returns the best match whatever it scores.}
}
\value{
A data frame with `given_entity`, `alethia_prediction` and `alethia_score`.
  Scores are rescaled to `[0, 1]`.
}
\description{
The counterpart of Python's `run_rapidfuzz_matching()`: for each query, the
highest-scoring reference.
}
\examples{
match_by_strings(
  c("Bombay", "Calcutta"),
  c("Mumbai", "Kolkata", "Chennai")
)

# Demand a close match, and report anything weaker as no match.
match_by_strings("Bombay", c("Mumbai", "Kolkata"), threshold = 0.9)
}
